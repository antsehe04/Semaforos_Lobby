import java.util.concurrent.Semaphore;

public class Jugador implements Runnable {

    int id; //hilo hijo en ejecucion

    public Jugador(int id){
        this.id = id;
    }
    @Override
    public void run() {
        String nombre="Jugador "+id;
        if
    }
}
